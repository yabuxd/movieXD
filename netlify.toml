[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback so React Router routes work on direct visits and refresh
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
